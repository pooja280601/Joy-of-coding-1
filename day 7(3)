#include <stdio.h>
#include<string.h>
void main()
{
    char a[50];
    int cnt=0;

    printf("Enter string: ");
    gets(a);
    
    for(int i=0;i<strlen(a);i++)
    {
        if(a[i]==' ')
        {
            cnt++;
        }
    }
    cnt=cnt+1;
    printf("the word count is :%d\n",cnt);
}
