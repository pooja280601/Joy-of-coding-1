#include <stdio.h>
#include<string.h>
int main()
{
  char a[1000], b[1000];
  int i, j;
  printf("enter a string :\n");
  gets(a);
  printf("reverse of the string is: ");
  for(j=0,i=strlen(a);j<=strlen(a);j++,i--){
      b[j] = a[i];
      printf("%c",b[j]);
  }
}

